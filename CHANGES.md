# Changes I Need

> Scratch list for changes I want made to Crownfall. Add items freely — one bullet is fine.
> Claude reads this file to pick up work. Anything under **Done** is finished and can be deleted.

**How to use:** jot the change under the right area. If you know the card / screen / file, say so — it saves a hunt. Priority tags optional: `[now]` `[soon]` `[whenever]`. `(large)` = multi-wave effort, worth scoping first. `❓` = Claude needs a detail before acting.

---

## 🔥 Open — things I want changed

### Table designer / editor
- **#14 follow-ups** — the one-screen shell shipped (see Done); deeper unification remains: canvas-native section editing (click a zone on the canvas to edit its zone def, cards filmstrip, rules attached to elements) — overlaps #12.
- **#9 — Vector drawing of custom shapes.** `(large)` A pen/vector tool for arbitrary shapes, beyond the preset shape kinds.
- **#8/#10 leftovers** — drag-to-reorder inside a flow container directly **on the canvas** (works via Layers panel today); migrate the **desktop** supply to the new layout system.

### Nodes / scripting
- **#13 — Fix the nodes; make them more functional + more of them.** The block library needs to be broader and more diverse — more block kinds covering more mechanics/UI, and the existing ones made more capable.
- **#12 — Deeper scripting ↔ UI integration.** `(large)` Wire the block system to the visual layer: control the **announce** banner (style/position/duration), **custom card UI**, **button → script** bindings, and more.

### Cards / rules (Forge)
- **#7 — Cards of type Treasure (and Victory) don't render.** ❓ **Could not reproduce** (2026-07-06): checked the Codex, the editor card list, a NEW card created with type Treasure, the designer canvas piles, and a live game — every type renders. Most likely your local Dominion was the stale seed-v8 copy; the seed bump shipping now replaces it. **If it still fails after this deploy, note exactly where you see it (which screen + which card).** The "more refined card layout control" half stays open (folds into #14's card editor work).

### Game runner / setup
- **#2 leftovers** — the kingdom picker shipped (see Done). Still open from the original ask: building **brand-new piles** at launch and per-pile options (pile size, cost overrides) — those are card-authoring features and fold into the Forge editor rather than the setup screen.

---

## 🔥 Open — Deckhand follow-ups (from the pasted table-builder)
- **Card tray** — Deckhand's bottom card strip: drag a card from the tray onto any holder on the canvas to seed its starting contents (writes deck entries). Big authoring win, needs canvas drop→DeckDef wiring.
- **Snap-in combos** — drop a label onto a button to set its text, a button onto an element to give it tap actions, anything into a panel to auto-stack.
- **More Deckhand elements** — dice, token — need engine-backed equivalents to stay honest with play. (Counter shipped — see Done.)

## 🔥 Open — Tablewright / Card-UI-Designer follow-ups (from the two pasted prototypes)
- **Guard rules ("before" timing)** — Tablewright's killer mechanic: a rule that runs while a move is PENDING and can **veto** it (with a log reason) or **redirect** the card elsewhere (Possession-style). Our stack/response system covers reactions; a declarative before-gate on `cardEnterZone` would replace hand-rolled legality in many defs and power "illegal play bounces back with a reason".
- **Scenes / overlay screens** — Card UI Designer's start screen & popovers: authorable overlay panels (own scene vs on-card popover, open on start / from a script block), on top of today's selector-switching. Pairs with a `openScene` block.
- **Hold-to-inspect card info panel** — hold a card at the table → pinned info panel (name/type/keywords/rules/flavor from template fields), with the progress ring. No card-inspect affordance exists today.
- **More interactive elements** — toggle (boolean variable switch), slider, order-list (drag to set resolution order), card-select battery (min/max + eligibility Expr + confirm) as engine-honest choice surfaces; roster (players/AI) belongs to the setup screen instead.
- **Sentence view for WHEN/zones** — the rule header (event + condition) already reads as a sentence; zone routes ("hand-moves: only to Discard of the owner") and per-phase action whitelists could adopt the same inline-blank prose.

## ✅ Done — Site-wide review: all 59 verified findings fixed (SEED_VERSION → 14)
One sweep across the whole platform, from the review report in `docs/site-review-2026-07-12.md`. The heavy hitters:
- **Bugs**: the Codex now reseeds on codex-only visits (the real cause of stale-card sightings); failed autosaves show a red ⚠ with Retry + Export instead of an eternal "Saving…"; online rooms close when the match starts, pending rooms cancel on leave/local-start, extra joiners get a clean refusal, peer disconnects get their own non-dismissible overlay, and a lockstep state-hash now catches desyncs; the scoreboard/opponent chips no longer leak bookkeeping variables (Scratch/Immune/etc. are `hidden` in the def); face-down cards keep their names out of picker titles.
- **UX**: destructive deletes everywhere get styled confirms (actions, rules, cards, templates — and leaving a live table); Actions/Rules collapsed to compact expandable rows in the Systems style; card list search; Issues rows click through to the owning panel; read-only mode actually disables controls; the log drawer, modals and dialogs got proper manners (Esc, focus trap, aria); marquee selection on the canvas (Space/middle-drag pans); discarded custom screens are restorable; a ? shortcuts popover; kingdom-picker selection gets a ✓; room codes get a copy button; disabled Start/Host explain themselves inline; the first hotseat turn skips the curtain; snackbar announcements queue instead of vanishing.
- **The hall**: honest copy everywhere (Invite/Watch/footer/fixture stats marked as hall lore), login remembers where you were headed, Create-account CTAs open the oath tab, new members get told about renaming, per-route document titles.
- **Performance**: the landing bundle dropped 387→233 kB and the forge shell 430→2.7 kB — the editor (345 kB), the play route (104 kB), the Dominion def builder (145 kB) and the Codex each load only when entered; whole-def validation is deferred off the keystroke path; autosave skips unchanged serialization; deleted games sweep their per-game device keys.

## ✅ Done — Supply chrome inside the card + real size levers in focus
- **Cost badges and × counts now live INSIDE the card face**: the round cost badge sits in the top-left corner and the × N pill in the bottom-left (bottom-right belongs to the keyboard digit badge) — nothing hangs off the edge, nothing gets clipped by the zone bounds, and the type line ("ACTION – ATTACK") stays readable. Editor mocks mirror the same geometry (the DGT diamond on compact tiles keeps its signature overhang — skins reserve room for it).
- **The Card chrome panel grew six steppers** — Card size, Gap, Columns, Rows, Box width, Box height — so you can resize and space the cards without leaving focus. Honest physics note included: cards never overflow the element's box (that's what killed the scrollbars), so past the fit limit the size levers are the box itself and fewer columns/rows — both now one click away in the same panel.

## ✅ Done — Systems page: everything addable on one screen
- **Phases + Zones + Variables now live together** on one wide "Systems" page (the Card-Forge style you pasted): three side-by-side panels, each with an **+ Add** in the header and compact inline-editable items with × delete — no more opening Zones, Vars, and Flow one at a time. The rail shrank from 9 sections to 7.
- Phase **scripts and allowed-action lists need width**, so phase rows stay compact (number, name, mode, reorder, delete) and an "N actions · N script blocks — edit ›" button opens a **full-width detail panel below the grid** with the block-script editor and action checkboxes. The **Setup script** is a pinned row at the top of the phases column and opens the same way.
- Everything the old three panels could edit survives: zone owner/visibility/layout/area with hints and the delete-warning modal, variable scope/type/adaptive initial value/status-bar visibility, phase reordering.

## ✅ Done — Designer: "👁 Hidden" ghosts — every element findable, phase buttons editable in focus
- **Why you couldn't see the landmark box**: the canvas's live preview resolves `visible` conditions against a sample game — and in a fresh sample the landscapes zone (and exile mats, duration strips, coffers/potion/debt chips…) are EMPTY, so all those occupied-only elements were invisible and unclickable. **Fix**: a "👁 Hidden" toggle on the Board-state bar ghosts them (dashed outline + name tag, hover to firm up) — 20 previously stranded elements on the Dominion screen are now one click away. Screen level shows only ƒx-conditional elements; the seal's per-state chrome stays out of the way there.
- **In focus, EVERYTHING hidden ghosts**: drill into the Phase seal and all 15 off-state children — the phase BUTTONS (Done buying / to cleanup / End the night), every seal name and hint — appear as clickable ghosts, each selecting into the properties panel. That was the "can't make the phase button in focus" gap.
- **"Editor wrong until I play"**: investigated with three targeted reproductions (fresh profile, stale stamp + stale stored def, editor-first entry) — the current editor route reseeds correctly in every case (ForgeApp re-stamps before the editor reads). The likely historical cause was a long-lived tab from before a deploy; a page reload (which playing tends to trigger) picks up the new bundle and reseeds. No code defect found on current code — if you see it again, note whether the tab had been open across a deploy.

## ✅ Done — Dominion wave 11: NOCTURNE complete — Night phase, Boons/Hexes, Heirlooms (SEED_VERSION → 27)
- **The Night phase is real**: a fourth turn phase between Buy and Cleanup (fourth seal dot and all) that only exists while Night cards are in the def — and only *appears* when you actually hold one, because "end the night" is gated on holding a Night card, so nightless hands auto-skip it invisibly. All 14 Night cards play there at no Action cost (Cobbler, Crypt, Raider, Vampire↔Bat exchanges, Guardian, Ghost Town…).
- **All 33 Nocturne kingdom cards** with the full supernatural apparatus: the shuffled **Boon and Hex decks** (all 24 implemented individually — Misery's −2/−4 VP is exact, Envy's Silver-nerf is enforced, The River's Gift pays at that turn's cleanup), **states** as real flags, **Spirits** (Will-o'-Wisp/Imp/Ghost) + **Wishes** + **Bats** as non-supply stock, **7 Heirlooms** swapped for a starter Copper only when their kingdom pile is on the table, and Necromancer's three **Zombies seeded into the trash**. Skulk hexes through a Moat-able window.
- Integration caught real cross-agent seams: a doubly-declared spirit stock, and a Haunted Mirror pointed at a ghost zone that didn't exist — which would have handed out Will-o'-Wisps instead of Ghosts (now name-selected). New lobby set: **Midnight Vigil**. 116 new engine probes; suite 1540 green; picker now spans **13 expansions / 293 piles**.

## ✅ Done — Dominion wave 10: ADVENTURES complete — Tavern, Travellers, pile tokens (SEED_VERSION → 26)
- **All 30 Adventures kingdom cards.** The **Tavern mat** is real: Reserve cards park on it when played and offer their calls at the printed windows (Ratcatcher/Guide/Transmogrify at turn start, Coin of the Realm and Royal Carriage reacting to Action plays, Duplicate to gains, Wine Merchant discharging at buy-end); Distant Lands scores its 4 VP only on the mat. Both **Traveller lines run end to end** — Page→Treasure Hunter→Warrior→Hero→**Champion** (parks forever: +1 Action aura + genuine attack immunity, probed against Militia) and Peasant→Soldier→Fugitive→Disciple→**Teacher** (parks and places pile tokens) — with the eight upgrades as real 5-copy exchange stock.
- **The duration half**: Amulet/Dungeon both-turns effects, Gear's set-aside, Haunted Woods and Swamp Hag watching from the duration zone, Bridge Troll, and **Hireling drawing an extra card every turn for the rest of the game**, probed across three turns.
- **Pile tokens ship** (adventuresTokens): Lost Arts/Training/Pathfinding/Seaway park a +1 Action/+$1/+1 Card/+1 Buy token on a pile of your choice, and Plan's Trashing token offers a trash on every gain from its pile — each bonus firing only for its owner, only on the marked pile. Ferry (per-player cost mod) and Inheritance (card morphing) are excluded honestly; Mission stays in the extra-turn bucket.
- New lobby set: **Roads and Taverns**. 64 new engine probes; suite 1423 green. Adventures joins the picker chips (All 260).

## ✅ Done — Dominion wave 9: MENAGERIE WAYS + ADVENTURES EVENTS (SEED_VERSION → 25)
- **Ways are real**: every hand Action now offers "Play as a Way" whenever a Way sits on the sideboard — the card enters play with its own effect **silenced** and the Way's effect fires instead (a lone Way auto-resolves; two prompt). Under the hood `onPlay` became play-tag-filtered engine-wide (audited, all suites green) — which also permanently closes the duration-march re-fire bug class. **18 of Menagerie's 20 Ways ship** (Butterfly's exact-cost return, Camel/Worm exiling from the supply, Turtle parking the card for a next-turn free replay, Seal's topdeck watcher…); Chameleon and Mouse are excluded honestly. The authoring agent corrected two card texts my brief had misquoted (Rat, Squirrel) against the printed faces.
- **12 of Adventures' 20 Events ship** (the token-free ones): Alms, Borrow, Quest, Save, Scouting Party, Travelling Fair, Bonfire, Expedition, Pilgrimage (Journey token as a real flip), Ball, Raid, Trade — with the −1 Card/−$1 mini-tokens approximated as next-redraw/next-Treasure effects (documented). The 7 pile-token Events (Ferry, Plan, Seaway, Lost Arts, Training, Pathfinding, Inheritance) wait for the token system; Mission waits for extra turns.
- 44 new engine probes; suite 1358 green. Landscape sideboard now offers **81 picks** across four kinds.

## ✅ Done — Dominion wave 8: RENAISSANCE — 25 cards + 17 Projects + Artifacts (SEED_VERSION → 24)
- **Projects work**: click one on the sideboard during your buy phase to claim it — pays, sets your flag (the scoreboard shows ownership), refuses re-claims, and the standing effect fires only for you from then on. All probed: Pageant's Coffers, Academy/Guildhall's per-gain banks, Citadel's first-Action double, Piazza's turn-start reveal-play, Road Network drawing off YOUR opponent's Victory buys, Canal's owner-turn discount, Sinister Plot's token bank. Excluded honestly: Fleet (extra turns), Star Chart (no shuffle event to hook), Capitalism (one-primary-type world).
- **All 25 Renaissance kingdom cards** — the **Villagers machinery earns its keep** (Acting Troupe, Lackeys, Recruiter, Sculptor bank them; the spend button was already live), and the five **Artifacts** (Flag, Horn, Lantern, Key, Treasure Chest) pass between players as they're earned: Flag Bearer's holder redraws six, Treasurer's Key pays $1 each turn, Swashbuckler's Treasure Chest pours Gold. Cargo Ship and Research are real Durations (Research with its own set-aside mat that still scores). Old Witch and Villain attack through the response window.
- New lobby set: **City of Inventors**. 67 new engine probes; suite 1313 green. The wave was killed once mid-flight by a session usage limit — the half-written files were deleted and the wave re-run clean.

## ✅ Done — Dominion wave 7: EMPIRES landscapes — 21 Landmarks + 13 Events + DEBT (SEED_VERSION → 23)
- **The landscape picker is live**: 34 Empires landscapes join the setup screen (pick up to 2), and the table grows a LANDSCAPES strip — live-verified picking Delve + Fountain and starting a game.
- **All 21 Landmarks**: gain-triggered VP awards (Aqueduct's exact Silver/Gold wells, Arena's discard-an-Action, Battlefield, Basilica/Colonnade on the 2022 errata wordings…) and game-end scorers (Museum's exact distinct-count, Palace's Copper-Silver-Gold sets, Wall, Wolf Den, Fountain, Keep…), every one gated on actually being picked. Documented approximations: the 6-VP-per-player pools are one shared 12-VP well, Mountain Pass's auction became a one-shot 8-debt-for-8-VP offer to the first Province gainer, Obelisk locks onto the costliest Action pile.
- **All 13 Events + real DEBT**: buy Events straight off the sideboard (they stay for rebuying — Delve chains Silvers by design); Dominate, Ritual, Salt the Earth, Windfall exact; **debt is a first-class currency** — Triumph/Annex/Donate/Wedding put you in the red, NOTHING is buyable while you owe, and a "Pay $1" button pays it down (red chips both layouts). Donate resolves its trash-party immediately (printed: after the turn); Tax marks one pile instead of the printed per-pile tokens — both in the module headers.
- 47 new engine probes; suite 1245 green. The Empires KINGDOM (30 cards) waits on split/gathering piles, honestly roadmapped.

## ✅ Done — Landscape engine core: Events + Landmarks machinery (SEED_VERSION → 22)
- **The sideboard exists**: a shared visible landscapes zone + hidden stock, a pure `pickLandscapes` transform (pickKingdom's sister), `landscapeCatalog`/`activeLandscapes`, and a **"Buy an event"** action gated to your buy phase — pays coins (never Bridge-discounted, per the rules), spends a buy, and fires the Event's effect **in place** so it stays buyable. Landmarks need no action: modules gate their scoring on presence in the zone. Expansion modules declare landscapes via one new `landscapes:` field.
- **Setup picker + table strips shipped dormant**: a "Landscapes" section appears under the kingdom picker (pick up to 2, Events show their cost) and occupied-only strips join both layouts — all invisible until the first real landscape set registers. Proven by a synthetic test module: 5 engine probes pin buy-phase gating, pay/fire/stay, repeat-refusal without buys, and landmark scoring that appears/disappears with the pick.
- **Design fork resolved for later waves** (docs/landscape-engine-design.md): Ways need an onPlay tag-audit before the play-substitution action; Projects will ship as two-copy cards with per-card owner vars. Next content: Empires' 21 Landmarks (expressible today), then Debt (unlocks Empires' 13 Events), tokens (Adventures), Night (Nocturne).

## ✅ Done — Dominion waves 5+6: ALCHEMY + MENAGERIE — 41 cards, Potions, Exile, Horses (SEED_VERSION → 21)
- **Potions are a real second currency**: every card carries a potion-cost field, the buy action enforces AND spends it, potions brew via the Potion treasure (+1 per play, reset at cleanup, status chips on both layouts), and the 16-copy **Potion pile promotes itself into the supply** whenever the picked kingdom contains an alchemical card — live-verified on the new *The Philosopher's Trail* set. Also fixed in passing: the Black Market would have sold Potions/Platinum from under the counter — basic reserve stock is now off the market.
- **Alchemy complete minus one** (11 cards): Familiar, Golem (digs and plays both Actions through a real response window), Scrying Pool, Alchemist's cleanup topdeck, Philosopher's Stone, Vineyard scoring, University, Apprentice (potion-cost cards draw the +2). **Possession excluded honestly** — controlling another player's turn needs the extra-turn engine (Outpost's bucket).
- **Menagerie complete** (30 cards): the **Exile mat** is a real per-player zone with table strips that appear only while occupied (yours bottom-right of the kingdom, the foe's floating top-right, mobile overlay), exiled cards still score at game end, and gaining a copy offers the printed discharge. **30 Horses** live as non-supply stock — Sleigh/Supplies/Cavalry/Groom/Hostelry/Livery/Paddock gain them, and each Horse returns to its pile on play. Durations: Barge (now-or-later), Mastermind (triple-play next turn), Gatekeeper (exile-watch attack). Prominent deviations: Snowy Village can't suppress later +Actions (announced instead), Cavalry doesn't rewind to your Action phase, dynamic costs (Fisherman/Destrier/Wayfarer/Animal Fair's alt-cost) ship at printed price — all in the module headers.
- Three new lobby sets: **The Philosopher's Trail**, **The Wild Ride**, **Into Exile** (17 total). Chips: **All (205)**, Alchemy (11), Menagerie (30). 66 new engine probes; suite 1193 green.
- **Known gap queued**: module mats older than Exile (Island, Native Village, Church) play correctly but have no table surface yet — the universal mats dock rides with the landscape engine.

## ✅ Done — Dominion wave 4: HINTERLANDS 2E + PROMOS — 33 cards (SEED_VERSION → 20)
- **All 26 Hinterlands 2E cards** — the on-gain/reaction set: Fool's Gold (first-play $1 / later $4, Province-reaction), Tunnel/Trail/Weaver (react to real discards — the cleanup sweep provably doesn't trigger them), Guard Dog (plays itself into an incoming attack), Trader's Silver exchange, Scheme's cleanup topdeck, Highway's discount, Berserker self-playing when bought with an Action out, Farmland's exact-$2 on-buy remodel, Border Village, Witch's Hut and the rest. Chips: **All (164)**, Hinterlands (26), Promos (7).
- **Seven playable Promos**: Envoy, Governor, Walled Village, Dismantle, Marchland, and two real **Durations — Church** (its own set-aside mat, parked cards still score) and **Captain** (commands an action straight off the supply pile, leaving the pile intact — a commanded Militia opens a genuine response window). Excluded honestly, pending mechanics: Stash (deck positions), Sauna/Avanto (split piles), Prince (permanent set-aside); Black Market has shipped in the core def all along.
- Two new lobby sets: **The Silk Road** (pure Hinterlands) and **The King's Envoy** (the promos with Base support). 62 new engine probes; suite 1124 green. Deviations per module header (Weaver is Action-typed in our one-primary-type world, Highway's "while in play" rides the per-turn discount, Guard Dog must be actively played like a Moat reveal).

## ✅ Done — Dominion wave 3: PROSPERITY 2E + CORNUCOPIA + GUILDS — 51 cards (SEED_VERSION → 19)
- **Three sets at once** — the catalog now spans **131 piles across six expansions**: Prosperity 2E (25: Anvil→Peddler incl. King's Court, Bank, Grand Market, Watchtower), Cornucopia (13 + the five **Prizes** as a real non-supply stock Tournament digs into), Guilds (13). Picker chips: All (131) / Base (28) / Intrigue (26) / Seaside (26) / Prosperity (25) / Cornucopia (13) / Guilds (13). Three new lobby sets: **The Royal Mint**, **Bounty of the Hunt**, **Arts and Crafts**.
- **New core mechanics, shipped once and shared**: per-player **VP tokens** (Bishop/Monument/Collection bank chips that ride every recount), **Coffers** (Guilds' coin tokens — banked by cards, spent one at a time via a real "+$1" status-strip button gated to your buy phase; Renaissance's **Villagers** got the same machinery ahead of time), and **non-supply piles** (module-declared stock outside the catalog/picker/pile-watcher — Prizes today; Horses, Spoils, Loot tomorrow).
- **Exact distinct-name counting** (Fairgrounds' 2VP-per-5-names, Menagerie's no-duplicates reveal, Horn of Plenty's cap, Harvest) implemented EXACTLY via per-card mark sweeps — not approximated. Deviations that ARE approximations are documented per card in the module headers (the honest list: Grand Market doesn't enforce its no-Copper buy rule, Peddler costs a flat 8, Quarry rides the global Bridge discount, Young Witch has no Bane pile, overpay resolves as post-buy "+$1 more?" prompts capped at $8).
- 84 new engine-probe tests; suite now 1062 green. All four modules were authored in one parallel agent wave against the live engine.

## ✅ Done — Dominion wave 2b: SEASIDE 2E — 26 cards, Durations live (SEED_VERSION → 18)
- **Durations work**: play a Wharf and it parks in the new **Set aside** strip through cleanup, fires its second half at the start of your next turn, marches back to In Play and discards normally. Table strips: your harbor-band SET ASIDE panel, a floating foe strip (incoming Wharf draws are public), and a mobile overlay. Parked cards still score VP at game end; Throne-Roomed durations repeat the now-half but park once (probed).
- **All 26 Seaside 2E cards** (Outpost excluded — extra turns are real engine work, still on the roadmap): Haven, Caravan, Fishing Village, Lighthouse, Merchant Ship, Wharf, Astrolabe (a Treasure-Duration), Bazaar, Warehouse, Cutpurse, Sea Witch, Corsair, Blockade, Monkey, Pirate, Smugglers, Lookout, Sea Chart, Island + Native Village (each with its own per-player **mat**), Tactician, Treasure Map, Treasury, Salvager, Sailor, Tide Pools. Two new lobby sets — **High Tide** and **Deep Currents** — both proven by seeded full-game playthroughs with card conservation. Deviations documented per card in the module headers (notably: Lighthouse waves off attacks Moat-style from the strip; Blockade/Corsair's ongoing watchers are one-shot approximations; Smugglers tracks the opponent's last qualifying gain by name).
- The authoring wave also **caught and fixed a real kit bug**: the duration march-back was tagged as a play, which re-fired the on-play half and looped the card between zones forever — now tag-filtered ('play' vs 'duration_return'), with a regression probe.

## ✅ Done — Dominion wave 2a: expansion picker + Prosperity basics (SEED_VERSION → 15)
- **The kingdom picker now knows its sets**: filter chips — All (54) / Base (28) / Intrigue (26) — scope the card grid to one printed expansion, composing with search and the preset chips. New expansion modules feed the chips automatically (each declares its `setName`).
- **Prosperity resources toggle**: a checkbox in the Kingdom panel adds **Platinum** (cost 9, worth 5 coins, ×12) and **Colony** (cost 11, 10 VP, ×8) to the supply, adds the *Colonies exhausted* end condition, and counts both piles for the three-pile rule. Pure def transform (`withProsperityBasics`), works with any hand-picked kingdom, ships to online guests via the host def, fully unit-tested (toggle round-trip, pickKingdom interplay, live-engine supply probe).
- **The rest of your ask is real engine work, mapped below**: Durations (Seaside), buyable non-card landscapes (Events/Landmarks/**Projects**/Ways), Night, Debt, Exile, and the mats. The roadmap section now carries the design notes; card authoring resumes set-by-set as those mechanics land — complete sets only, same bar as Base/Intrigue. (This wave's multi-agent authoring run was cut short twice by session usage limits — Seaside is queued first.)

## ✅ Done — Deep focus editing: card chrome, group drill-in, reusable components
- **Focus a supply → edit the cards' chrome.** Focusing a zone now shows a **Card chrome** panel on the canvas: click the cost badge, × count, tile name, caption or empty note on the mock and the inspector opens that part's editor — hidden toggle, text colour, size, bold, plus full box styling (fill/border/radius/shadow). Overrides live per zone element (`partStyles`), render in the real game (runner + editor mocks + anatomy all WYSIWYG), and only touch the zone you edited — the treasure column keeps its skin while your kingdom's cost lozenges go green. An **✎ Edit card design** button jumps straight to the Cards panel for the face itself.
- **Groups edit like documents.** Selecting a group (e.g. the Phase seal) lists **every child** in the inspector — click a row to edit that child in full detail, ⛶ to dive into the group. All 19 seal children (plate buttons, phase dots, name/hint texts) were already editable via focus mode; now they're discoverable.
- **Save anything as a reusable component.** ⬡ Save now offers overwrite when the name exists; a **multi-selection** can be saved as one grouped component without touching the screen; palette entries gained ✎ rename; and inserting a component into a game that lacks its bindings (actions/zones/variables/phases) warns first — powered by a new full-Expr reference walker (`collectDefRefs`/`missingDefRefs`, unit-tested against a seal-like fixture).
- Caveats: chrome drawn by CSS `::before` (the lozenge's rotated square) takes colour/size but not background overrides; card-face internals (name/art on the card) remain the card designer's domain — the ✎ bridge takes you there.

## ✅ Done — #2: Kingdom picker on the game setup screen
- The Dominion setup menu now has a **Kingdom panel**: all 7 preset sets as one-click chips (active set highlighted), a **🎲 Random 10** roll, a search box (matches name *or* type line, e.g. "attack"), and a browsable grid of **all 54 kingdom cards** (cost badge + type line) — toggle tiles to hand-pick exactly 10. A count chip tracks progress; **Start** and **Host a room** stay disabled until the kingdom is complete, and the host's picked kingdom is what both online players get (the host def is authoritative). Unpicked piles wait in the reserve, so the Black Market keeps its stock. Works for any def whose setup swaps kingdom piles — custom Forge games that follow the pattern get the picker for free.

## ✅ Done — Dominion catalog wave 1: complete Base 2E + Intrigue 2E (54 kingdom piles)
- **36 new cards** (SEED_VERSION → 12): the ten missing Base 2E cards (Harbinger, Merchant, Vassal, Bureaucrat, Moneylender, Poacher, Bandit, Library, Sentry, Artisan) and ALL 26 Intrigue 2E cards (Courtyard → Upgrade, incl. Bridge's cost reduction, Duke's per-Duchy VP, Diplomat's own reaction, Minion/Torturer/Swindler/Replace attacks). Four new lobby kingdom sets: **Deck Top**, **Underlings**, **The Grand Scheme**, **Masters of Deceit**. Every set proves itself in a seeded full-game playthrough with card conservation + from-scratch VP recount; 76 new deterministic per-card probe tests.
- **Architecture**: cards now live in per-expansion modules (`src/forge/dominion/*.ts`) coding against a typed CardKit — new expansions plug in without touching the core def builder. Engine gained `repeatWhile` (Library) plus shared infrastructure: a hidden look/set-aside staging zone and turn-scoped **cost-discount** wiring (buy legality, payment clamped at $0, gain caps, cleanup reset).
- **Known deviations** (documented per card in the modules): single primary type per card (Nobles/Mill are Action-typed, Harem Treasure-typed — their printed VP still scores; type-sensitive effects see the primary type), Masquerade passes sequentially rather than simultaneously, "put back in any order" is fixed-order, Wishing Well names supply piles only, Courtier may repeat bonus picks, Throne-Roomed Merchant/Minion/Replace have small multi-play quirks, Secret Passage inserts top/bottom only.

## 🔥 Open — the remaining Dominion expansions (engine roadmap)
Each set below needs the named engine mechanic before its cards can be faithful; everything else in it is already expressible:
SHIPPED so far: Base 2E, Intrigue 2E, Seaside 2E (Durations, mats; Outpost excluded pending extra turns), Prosperity 2E (VP tokens; basics toggle), Cornucopia (Prizes), Guilds (Coffers), Hinterlands 2E, Promos (7 of 11), Alchemy (Potions; Possession excluded pending extra turns), Menagerie kingdom (Exile mat + Horses; Ways wait on landscapes). Still open, in build order:
- **Landscape engine** `(large)` — buyable/visible non-card sideboard: Events (buy-time scripts), Landmarks (scoring hooks), Projects (persistent per-player flags), Ways (play substitution), Traits/Prophecies; setup-screen landscape picker. Unlocks the sets below.
- **Adventures** — Tavern mat (reserve-with-call), adventure tokens, Travellers (pile-exchange lines), Events.
- **Empires** — Debt (negative-coin buys), Landmarks, split/gathering piles.
- **Nocturne** — Night as a fourth phase (PhaseDef works today); Boons/Hexes decks; Heirlooms.
- **Renaissance** — Villagers machinery SHIPPED (bank + spend button); Projects (landscape), Artifacts (pass-around markers).
- **Dark Ages** — on-trash triggers work today; Ruins/Shelters/Knights need mixed/randomized piles; Spoils/Madman ride the non-supply mechanic.
- **Allies / Plunder / Rising Sun** — Favors + Ally cards + rotating split piles; Loot deck + Traits; Shadow cards + Prophecies + Debt events (furthest out).
- **Outpost (Seaside)** — extra turns: a real engine block (turn-order injection), queued with the heavy engine work.

## ✅ Done — auto-resolve, centered carousels, ONLINE MULTIPLAYER
- **Forced choices resolve themselves**: any request with exactly ONE valid answer (single mandatory card/pile candidate, a lone option, a lone player target, min = max = every candidate) never opens a sheet — the engine answers it with a log line ("… only one option; resolved automatically"). Revealed choices are exempt (the sheet is the reveal), optional picks and yes/no always ask. Phases where the current player has zero legal moves already auto-skipped; response windows already auto-passed — the table now never waits on a non-decision.
- **Mobile supply carousels center properly**: a row that fits the frame centers as a group; an overflowing row snap-centers every pile (the skin's old left-anchor override is gone).
- **Online multiplayer** (`Play online` on the game setup screen): host a room → share the 6-letter code → a friend joins from any browser. Peer-to-peer WebRTC (PeerJS public broker), no server, no accounts. Deterministic lockstep: the host's def + seed ship at the handshake (locally-edited games stay in sync), each client runs its own engine and relays moves/choice answers; each device is pinned to its own seat (no hotseat curtain) and a disconnect freezes the table with a banner. Follow-ups queued below.

## 🔥 Open — multiplayer follow-ups
- **Hall matchmaking is still theater** — wire the hall's "take a seat" ceremony (Summons/Tables) to real hosting/joining instead of the scripted fake foe.
- **Online rematch** — "Play again" currently returns to setup; a rematch handshake (new seed over the same link) would keep the room alive.
- **Reconnection** — a dropped peer freezes the table; resumable sessions need a move-log replay on rejoin.
- **>2 players online** — the transport is 1:1; a host-relayed star topology would generalize it.

## ✅ Done — table redesign + zone card-presentation controls
- **Zone elements got finer card control** (select any zone element — incl. focused): **Badge shape** (diamond ⇄ round cost badge), **× Count badge** (corner / pill under the card / hidden), **Card style (this element)** — a full fill/border/radius/shadow editor painted over every card face or pile tile that ONE element shows (dress the kingdom without touching the card template), and **Empty-state text** ("Play zone empty." — also gives pile boards an empty state). Editor canvas previews all of it (and its badge corners now match the runner's).
- **Flat hands fixed**: a 0° fan no longer reserves fan-arc headroom, so flat card strips size properly.
- **The war table rebuilt to the keeper's mock** (SEED_VERSION → 11): TREASURES / VICTORY / KINGDOM ACTION CARDS columns with colored underlined headers + gold hairline separators; the kingdom as a 5×2 grid of BIG card faces with round cost badges and bottom count pills; a status strip (phase seal plate + ACTIONS/BUYS/COINS chips); the "PLAYER'S REALM — ACTIVE TURN" header band; a PLAY ZONE panel that says "Play zone empty."; the striped DECK tile; DISCARD/TRASH tiles; a flat full-width HAND strip (every copy visible); CHRONICLE as a collapsed right-edge tab. All authored elements — fully editable in the designer.

## ✅ Done — Tablewright + Card UI Designer wave 1
- **Sentence scripts**: every script editor (Rules, Actions, Flow, card Abilities, button scripts) now opens as Tablewright-style PROSE — "then move cards [1] from [Deck] to [Hand]" with tap-to-edit blanks, gold keywords, indented then/else/do lanes, ↑↓/✕ row tools and "＋ effect" pickers. It's a second lossless projection of the same Block[] tree; the **Graph** view is one toggle away (choice remembered per device).
- **"Requires …" gates**: buttons (and counters) take an **Enabled when** condition on top of engine legality — while it fails the control disables and players see a `requires <condition>` tag naming it (e.g. "requires Coins of $viewer ≥ 5").
- **Counter element**: a live variable readout with −/＋ steppers where **every tick is a real engine action** (triggers fire, legality applies). The inspector's ⚡ one-click creates "<Var> +1/−1" actions, binds both steppers AND registers them in every manual phase (phases whitelist their moves).

## ✅ Done (delete once confirmed shipped)
- **Mockup shell** (`817617f`): one top bar — "Projects › name" breadcrumb, the canvas tools (Desktop/Mobile · aspect · undo/redo · zoom · Fit · fullscreen) portaled up from the canvas, save hint, ▶ Play, and a ⚙ gear that opens Info (Info left the rail). Right rail gained **SCREEN | INSPECTOR** tabs over the properties box. Board-state bar sits below the canvas. Crimson kept.
- **Deckhand card designer** (`b199210`): tool rail (Text/Stat/Image/Box/Circle/Line one-click), card canvas + element chip strip, ONE context-sensitive inspector — the four stacked side panels and the add-element modal are gone.
- **Deckhand UX merge** (`c734268`): stroke icon set across the section rail + palette; Deckhand-style icon tiles; one-click **Deck / Pile / Hand / Slot / Grid / Carousel** holders (each drops a ready game zone — def + element in one tap); block graphs now read as **WHEN / IF / DO** with color-coded tags and node spines. Crimson theme kept; real engine underneath.
- **#14 — ONE game-engine screen** (`0a91cd1`): the tab bar is gone. The WYSIWYG canvas is the editor; Info/Cards/Types/Zones/Vars/Flow/Actions/Rules/Filters slide over it as panels from a left section rail (heavy sections open wide, ⇤/⤢ toggles width, rail button or ✕ closes). Edit rules and watch the screen side by side.
- **#3 — drill-in selection** (`b56d03b`): click selects the top-level element; double-click steps into the group (repeat to reach the item); single clicks then pick siblings at that depth; Ctrl+click deep-selects instantly.
- **#4 — full keyboard set** (`b56d03b`): Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y undo-redo (real history with burst coalescing + toolbar ↶↷ buttons), Ctrl+C/X/V clipboard (works across variants/tabs), Ctrl+D duplicate, Ctrl+G/Ctrl+Shift+G group/ungroup — joining the existing Esc/Delete/arrow-nudge.
- **#6 — theme swatches** (`b56d03b`): ★ Save pins the current colour to a persistent "My theme" row in every colour picker; right-click removes.
- **Militia "cancel" prompt** — fixed in the engine (`8a8b3dc`): players with no possible response are auto-passed, so the attacker never sees a Pass prompt on their own attack; response windows land directly on players who can actually react (Moat/Counterspell/traps) and stacks with no possible responders resolve instantly.
- **#5 — no scrollbars on cards** (`32a1429`): cards in authored zones are measured + width-clamped to genuinely fit their rect (fans by height, grids by rows×columns); zone containers clip instead of scrolling.
- **#16 — hero CTAs** (`32a1429`): "Play Dominion" / "Take a seat" go straight to the Dominion setup screen (no login wall).
- **#1 + #11 — exact-WYSIWYG canvas** (`9513b84`): the ⚡/ƒx/⇥/count badge chips are gone from the canvas (still in Layers), and nothing is labeled "preview" anymore — context bar is "Board state".
- **#15 — Codex unified with the engine** (`da2b22c`): the Codex loads the real (keeper-edited) Dominion def and renders through the same CardView + template as the game; filter chips come from the def's types/tags.
- **Reusable "for each opponent" + "discard down to N" blocks**, Militia rebuilt to use them (`04fcb14`).
- **SEED_VERSION → 10**: pushes the rebuilt Militia + def fixes to existing games (overwrites local edits to the built-in Dominion — approved).
- **#8 (smart containers + typed slots) + #10 (robust grid)** — the "layout backbone" wave (merged `83fbc06`): `FlowLayout` on any container (Grid/Row/Column), typed slots, `panelSwitcher` + `image` elements; Dominion mobile supply migrated as proof. Leftovers tracked above.

## Fix: editor preview squished left until a table was visited

- Root cause: runner.css (the .rn-* positioning rules) was imported only by the play chunk (PlayPage/TableScreen). A session that opened the editor first rendered the live preview with position:static elements - inline left/top ignored, everything stacked at the left edge of the stage - until visiting any table injected the stylesheet for the rest of the session ("wrong until I have played a game").
- Fix: ScreenRenderer.tsx now imports its own stylesheet, so every chunk that renders .rn-* markup (editor preview included) ships the rules in dev and production.
- Guard: src/runner/rendererStyles.test.ts pins the import.
